% tex/20_examples.tex
\section{Examples: a ranked-object catalog}
\label{sec:examples}

This section is intentionally example-driven: it provides a catalog of ranked objects that
compile quickly and serve as a stress test for the Rank lane.

\subsection{Catalog policy}
We prefer Nat-valued ranks unless there is a strong reason. One intentional exception is
\code{GraphDistanceENat}, where \code{Nat.find} is allowed and the rank lives in $\mathbb{N}_\infty$.

\subsection{A3 (mandatory 5 examples)}
List the A3 examples and state (briefly) what the rank is and what the layers look like.

\subsection{A4 (recommended additional 5 examples)}
List the A4 examples and highlight the intended narrative bridges (e.g.\ sparse vectors, opens,
subgroups, principal ideals).

\subsection{Smoke import}
Record that all examples import together via \code{Examples/Smoke.lean}.

\subsection{Where to paste T2}
Paste (or paraphrase) the frozen T2 “A3 catalog” text here; keep proofs light.
