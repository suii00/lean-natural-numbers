会話を移行するためにプロンプトを作成してください


以下のプロンプトを次のアシスタントに渡してください（現状の作業文脈とやるべきことを全て含んでいます）。

— Prompt start —

あなたは Lean 4 + mathlib を用いたトポロジー開発のコーディングアシスタントです。対象リポジトリでは、ブルバキの「順序対と射（射影）化」を基調に、md から Lean ファイルを構築しています。現在の重点は C（Stone–Čech 統合）の完成です。A（Δ ⊣ × の同型・普遍性）、A′（指数法則、評価の連続）と B（被覆写像）は既に一定の骨格があり、C の統合を安定化させると以後の ext; simp ワークフローが全体で強化されます。

目標

src/MyProjects/Topology/B/TopologyB_Bourbaki.lean の C 節（Stone–Čech コンパクト化）を mathlib の StoneCech で一貫実装し、コンパイルエラーを解消する。
普遍性 API（lift/β規則/一意性）を ext; simp で使いやすい形に整える。
既存の A/A′/B の記述は壊さず、そのままビルド可能にする。
環境

mathlib import: Mathlib.Topology.Compactification.StoneCech を使用
連続写像は ContinuousMap 表記で統一（C(…) 記法は使わない）
Compact-open: import Mathlib.Topology.CompactOpen 済み
作業対象ファイル

src/MyProjects/Topology/B/TopologyB_Bourbaki.lean
C: Stone–Čech 骨格（structure + lift + trivial）
C': Stone–Čech from mathlib（fromMathlib 実装）
参考: A（TopCat の Δ と × の同型），A′（Homeomorph.curry），B（被覆写像スケルトン）
現在のエラー（抜粋と原因）

src/MyProjects/Topology/B/TopologyB_Bourbaki.lean:295:40 ほか
Function expected at StoneCechCompactification … → 構造体 StoneCechCompactification の定義「前」に、その構造体名を仮定する instance を定義しているのが原因。インスタンス宣言の位置を「構造体定義の直後」へ移動する。
:325:30 および :351:33
failed to synthesize TopologicalSpace S.βX → βX の位相構造が型クラスとして見つかっていない。StoneCechCompactification のフィールド instTopβX 等を使って、以下の3つのインスタンスを構造体定義の直後に宣言すること。
instance (S : StoneCechCompactification X) : TopologicalSpace S.βX := S.instTopβX
instance (S : StoneCechCompactification X) : CompactSpace S.βX := S.instCompβX
instance (S : StoneCechCompactification X) : T2Space S.βX := S.instT2βX
lift 系で “declaration uses 'sorry'” と見なされるケース
lift は Classical.choose (S.universal … f) をそのまま返す（let _ : TopologicalSpace S.βX := … は不要）。lift_comp/lift_comp_apply/lift_unique は Classical.choose_spec から素直に導く。
stoneCechExtend_stoneCechUnit の直接使用で型が合わない
stoneCechExtend_extends : stoneCechExtend hg ∘ stoneCechUnit = g（関数レベル）から ext x; simp [Function.comp] で点毎に落とす。simpa でなく simp or congrArg (fun h => h x) を使い、左辺右辺の型合わせをする。
連続写像等式と関数等式のすれ違い
stoneCech_hom_ext は関数レベルの等式を返すため、最後に ext y で ContinuousMap の等式に昇格する。
やってほしい作業（精密）

StoneCechCompactification 構造体の直後に、βX 用の型クラスインスタンスを定義
TopologicalSpace S.βX, CompactSpace S.βX, T2Space S.βX
これにより ContinuousMap S.βX K が everywhere で型クラス推論可能になる
lift/lift_comp/lift_comp_apply/lift_unique を Classical.choose とそのスペックから実装
余計な let _ : TopologicalSpace S.βX := … を挿入しない
simp と congrArg で点毎に落とす
fromMathlib の universal での β規則
stoneCechExtend_extends (hg := f.continuous) を使い、ext x; simp [Function.comp] で (F.comp ι) = f を示す
fromMathlib の一意性
G.comp ι = f = F.comp ι から (G ∘ stoneCechUnit) = (F ∘ stoneCechUnit) を得る
stoneCech_hom_ext（稠密像 + T2）で関数等式を得て、ext y で ContinuousMap の等式に昇格
既存のスモークテスト example が通ることを確認
((fromMathlib …).lift K f).comp (fromMathlib …).ι x = f x
インポート

すでに以下が入っています（必要に応じて調整）:
Mathlib.Topology.Compactification.StoneCech
Mathlib.Topology.CompactOpen
Mathlib.Topology.ContinuousMap.Basic
完了条件

src/MyProjects/Topology/B/TopologyB_Bourbaki.lean がビルド可能
C/C' の普遍性 API で ext; simp が機能
既存の A/A′/B に回帰がない
参考ファイル位置

構造体とエラー周辺:
src/MyProjects/Topology/B/TopologyB_Bourbaki.lean:286
:295, :298, :301（インスタンスの配置に起因するエラー）
:321 以降（lift 系）
fromMathlib 実装:
:400 以降
実行手順（推奨）

修正後に lake build（あるいはエディタからビルド）で確認
失敗時は該当行で set_option diagnostics true を使い、型クラス解決の不足を特定
— Prompt end —