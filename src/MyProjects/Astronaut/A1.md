Understood! I've prepared a single **submission-ready .lean template** for the topic 1 (**A\* Optimality**) section.

* The graph and path implementations are kept minimal, with **the essential properties of heuristics required for A\*** correctness—namely, admissibility and consistency—as well as **"true distance"-like functions** abstracted first.
* The "core theorems to prove" are marked with `TODO` and left blank within `by` (all others are as proven as possible).
* This foundation can later be reused by replacing "path and shortest distance concrete definitions" (such as the Dijkstra-like `distToGoal` construction) as needed.


## Usage Guidelines (Proposed)

* Make `admissible_of_consistent_and_goalZero` your first target for completion, then remove the `admit` directive.

  * The key inductive tool is "propagation of inequalities along length-n paths leading to the goal" (telescoping).
* After that, extend `a_star_optimality_spec` by defining `open/closed`, `g/h/f`, and the **extraction rule** (removing the minimum f) to reach the assertion that "g is shortest when the goal is initially expanded."

If needed, I can add example implementations of `neighbors` / `w` with **specific grid maps/rendezvous coarse-graining** to this template.
